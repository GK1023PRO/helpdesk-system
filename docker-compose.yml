version: '3.8'

services:
  web:
    build: .
    ports:
      - "5004:5000"
    environment:
      - DATABASE_URL=postgresql://admin:admin123@db:5432/helpdesk
      - MAIL_USERNAME=georgeskhousa03@gmail.com
      - MAIL_PASSWORD=kuyqnmakhtlhgxxj
    depends_on:
      - db
    volumes:
      - .:/app
      - uploads-data:/app/static/uploads

  db:
    image: postgres:15
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin123
      - POSTGRES_DB=helpdesk
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  postgres-data:
  uploads-data: